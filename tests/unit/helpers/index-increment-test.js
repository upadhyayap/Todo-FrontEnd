import { indexIncrement } from '../../../helpers/index-increment';
import { module, test } from 'qunit';

module('Unit | Helper | index increment');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = indexIncrement(42);
  assert.ok(result);
});
