import { getIndex } from '../../../helpers/get-index';
import { module, test } from 'qunit';

module('Unit | Helper | get index');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getIndex(42);
  assert.ok(result);
});
